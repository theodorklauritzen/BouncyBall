


function setup() {
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  background(255);

  fill(0);
  textSize(50);
  textAlign(CENTER);
  text("Home", width / 2, height / 2 - 25);
}
